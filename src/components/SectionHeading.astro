---
interface Props {
  title: string;
  description?: string;
  align?: 'left' | 'center';
}

const { title, description, align = 'center' } = Astro.props;
---

<div class:list={['max-w-2xl', align === 'center' ? 'mx-auto text-center' : 'text-left']}>
  <h2 class="text-3xl font-bold leading-tight text-balance sm:text-4xl">{title}</h2>
  {description && <p class="mt-4 text-base leading-relaxed text-ink-300">{description}</p>}
</div>
